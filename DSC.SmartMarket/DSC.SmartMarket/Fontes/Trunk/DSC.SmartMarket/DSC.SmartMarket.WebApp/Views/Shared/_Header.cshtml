<nav class="navbar">
    <div class="container-fluid">
        <header class="nav row">
            @if (Session.IsLogado())
            {
                <h1 class="navbar-brand">@Html.ActionLink("Smart Market", "Listar", "Dashboard")</h1>
                <ul class="nav navbar-nav">
                    <li>@Html.ActionLink("Clientes", "Index", "Cliente")</li>
                    <li>@Html.ActionLink("Produtos", "Index", "Produto")</li>
                    <li>@Html.ActionLink("Supermercados", "Index", "Supermercado")</li>
                </ul>
                <ul class="nav navbar-nav navbar-right">
                    <li class="dropdown">
                        <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                            <span class="glyphicon glyphicon-user"></span>
                            <strong>@Session.GetUsuarioLogado().Nome&nbsp;(@Session.GetUsuarioLogado().Email)</strong>
                            <span class="caret"></span>
                        </a>
                        <ul class="dropdown-menu">
                            <li>@Html.ActionLink("Dados do Usuário", "Detalhe", "Usuario")</li>
                            <li>@Html.ActionLink("Alterar Senha", "AlterarSenha", "Usuario")</li>
                            <li>@Html.ActionLink("Sair", "Sair", "Usuario")</li>
                        </ul>
                    </li>
                </ul>
            }
            else
            {
                <h1>Smart Market</h1>
            }
        </header>
    </div>
</nav>